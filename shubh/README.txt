login auth -> node node.js in command line and then open localhost:8080 in browser
----might have to download some npm modules

getPlaylists -> 'make' in the dir. but authcode in request.cpp needs to be replaced with the one in loginAuth/authCode.txt manually rn
